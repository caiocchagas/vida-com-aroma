generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String   @id @default(uuid())
  email         String   @unique
  name          String?
  password      String?
  role          String   @default("CUSTOMER")
  
  lifeGoal          String?
  mainComplaint     String?
  chronology        String?
  energyLevel       String?
  clinicalRestrictions String? // will store JSON or comma-separated string of array
  environment       String? // will store JSON or comma-separated string of array
  preferredMethod   String?
  
  hasPaid       Boolean  @default(false)
  nurturingSent Boolean  @default(false)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  transactions  Transaction[]
}

model Transaction {
  id            String   @id @default(uuid())
  userId        String
  user          User     @relation(fields: [userId], references: [id])
  paymentId     String?  @unique
  amount        Float
  status        String
  trackedDays   Int[]    @default([])
  createdAt     DateTime @default(now())
}

model Event {
  id        String   @id @default(uuid())
  name      String
  sessionId String
  metadata  String?
  createdAt DateTime @default(now())
}

model WebhookLog {
  id        String   @id @default(uuid())
  source    String   // e.g., "mercadopago"
  event     String   // e.g., "payment.created", "payment.updated"
  payload   String   // The entire raw JSON stringified
  createdAt DateTime @default(now())
}
